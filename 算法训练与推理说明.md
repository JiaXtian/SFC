# 动态卫星 SFC 编排算法说明（训练 + 推理）

## 1. 总体架构

当前系统采用 `GNN + Actor-Critic(A2C) + 启发式约束搜索` 的混合架构：

1. `GNN` 对当前拓扑做图编码，得到每个卫星节点的嵌入向量。
2. `启发式剪枝` 先过滤掉明显不可行节点（资源不足、链路不可达、时延/带宽不满足等）。
3. `Actor` 对候选节点给出概率分布，作为搜索优先级。
4. `后端约束引擎` 逐步验证路径、带宽、时延、可靠性，并提供多级 fallback。

这意味着模型不是“单点决策器”，而是“候选排序器 + 搜索加速器”。

## 2. 模型输入与结构

### 2.1 GNN 编码器

- 输入：
  - `node_features`: `[num_nodes, 8]`
  - `edge_index`: `[2, num_edges]`
- 输出：
  - `node_embeddings`: `[num_nodes, 192]`

节点 8 维特征主要包含：
- CPU 可用比、内存可用比、磁盘可用比
- 核心网络负载、节点可靠性
- 邻接链路活跃度、带宽余量、平均时延归一化

### 2.2 Actor 网络

- 输入：
  - `node_embeddings` (`192`)
  - 当前 `VNF` 特征（4维：cpu/mem/bw/disk）
  - `context_features`（48维）
- 输出：
  - 候选节点概率 `probs`
  - 原始打分 `logits`

当前后端与训练统一使用 `context_dim=48`，避免训练/在线口径漂移。

## 3. 训练的作用（为什么“没重训也能推理”）

即使不重训，后端仍会输出“看起来合理”的结果，原因是：

1. 后端会加载已有 ONNX 权重（历史模型）。
2. 后端还有强启发式与硬约束验证，不会完全依赖 Actor。
3. 若 Actor 输出不理想，仍可通过排序、路径重算、fallback 扫描得到可行方案。

训练真正提供的价值是：
- 在候选中更快把“全局更优节点”排到前面。
- 在动态拓扑波动下提高成功率与 SLA 满足率。
- 降低后端 fallback 次数与总体推理时延。

## 4. 动态训练闭环

动态训练采用“拓扑时间推进 + 请求时间推进”：

1. 读取 `topology_timeline` 与 `request_timeline`。
2. 每个决策步推进到当前 tick 的拓扑快照。
3. 在该快照下执行候选剪枝、GNN 编码、Actor 选点、路径验证、资源扣减。
4. 以 `episode reward + success/SLA` 反向更新策略。

动态场景关键因素已加入训练与后端一致口径：
- 节点/链路活跃比例
- 平均链路时延、带宽利用率
- 时间戳与拓扑版本
- 带宽硬约束（包含最终段到目的节点）
- 可靠性软化模型（长路径避免指数级坍缩）

## 5. 后端推理逻辑（在线）

在线推理核心流程：

1. 构建图输入并运行 GNN。
2. 对每个 VNF：
   - 过滤候选节点（资源、可达性、时延、可靠性预估）
   - 代价排序
   - Actor 对候选打分
   - 按融合分数依次探测路径与约束
3. 完成所有 VNF 后，计算末段到目的节点路径并校验 SLA。
4. 生成候选部署集，按综合得分返回 top-k。

为了减少“有解却报无解”，新增了两层兜底：

1. 候选筛选全空时，触发全局可达性恢复扫描（无 hop cap）。
2. VNF 内部 fallback 扫描覆盖全部已排序候选，避免被扫描上限截断。

## 6. 数据集与泛化策略

建议使用多规模动态数据混合训练（例如 800/1600/2500/4000/4800 星），并同时混入不同：
- 轨道高度/倾角
- 负载等级（low/medium/high/mixed）
- 故障概率组合
- 业务类型模板

项目已提供多规模动态数据生成脚本：
- `train/ground_training/data_generation/generate_dynamic_multiscale_data.py`

## 7. 关键产物

训练输出：
- `models/checkpoints/model_dynamic_best.pth`
- `models/checkpoints/gnn_dynamic_best.pth`
- `logs/dynamic_training_metrics.json/csv`
- `logs/dynamic_validation_report.json`

导出输出：
- `models/exported/gnn_encoder.onnx`
- `models/exported/actor.onnx`
- `models/exported/model_io_meta.json`

## 8. 一句话结论

训练并不是替代后端约束引擎，而是给后端提供“更优先的候选顺序”；后端负责最终可行性与鲁棒性，二者叠加后才能在动态拓扑下稳定给出高质量策略。
